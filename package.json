{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "lerna": "^3.21.0"
  },
  "scripts": {
    "build": "lerna --scope ${PROJECT_NAME} run build -- --dest ../../dist"
  }
}
